import React from "react"
import { render } from "@testing-library/react"
import SearchbarContainer from "../searchbarcontainer"

test("Displays the correct SearchbarContainer", () => {
  const { getByTestId } = render(<SearchbarContainer />)
  // Assertion
  // TODO: Add Test
  // --> Test will pass
})