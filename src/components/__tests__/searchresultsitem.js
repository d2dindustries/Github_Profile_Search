import React from "react"
import { render } from "@testing-library/react"
import SearchResultsItem from "../searchresultsitem"

test("Displays the correct SearchResultsItem", () => {
  const { getByTestId } = render(<SearchResultsItem />)
  // Assertion
  // TODO: Add Test
  // --> Test will pass
})