import React from "react"
import { render } from "@testing-library/react"
import SearchCounter from "../searchcounter"

test("Displays the correct SearchCounter", () => {
  const { getByTestId } = render(<SearchCounter />)
  // Assertion
  // TODO: Add Test
  // --> Test will pass
})