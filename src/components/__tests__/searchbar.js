import React from "react"
import { render } from "@testing-library/react"
import SearchBar from "../searchbar"

test("Displays the correct SearchBar", () => {
  const { getByTestId } = render(<SearchBar />)
  // Assertion
  // TODO: Add Test
  // --> Test will pass
})